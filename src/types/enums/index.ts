export enum BookmarkTarget {
    PLACE = 'Place',
    EVENT = 'Event',
}

export enum CategoryType {
    PLACE = 'Place',
    EVENT = 'Event',
}

export enum UserType {
    ADMINISTRATOR = 'Administrator',
    VISITOR = 'Visitor',
    RESIDENT = 'Resident',
}